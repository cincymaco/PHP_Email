This is plain text.

Some email clients will only display plain text, others will use plain text version for the message preview.

This data was recevied at {{ timestamp|date("Y-m-d g:i:s A") }}.

{{ data|json_encode() }}
